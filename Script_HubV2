local Material = loadstring(game:HttpGet("https://raw.githubusercontent.com/Kinlei/MaterialLua/master/Module.lua"))()

local X = Material.Load({
	Title = "scripts_hub",
	Style = 4,
	SizeX = 400,
	SizeY = 350,
	Theme = "Light",
	ColorOverrides = {
		MainFrame = Color3.fromRGB(235,235,235)
	}
})

local Y = X.New({
	Title = "UI's"
})

local Z = X.New({
	Title = "Hub"
})

local W = X.New({
	Title = "hub (keys)"
})

local C = X.New({
	Title = "discord"
})

local A = Y.Button({
	Text = "Epic",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/Minecraft18363/one-punch-man-Destiny/master/Epic"))();
	end,
	Menu = {
		Information = function(self)
			X.Banner({
				Text = "One Punch Destiny"
			})
		end
	}
})

local A = Y.Button({
	Text = "Lumber",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/Minecraft18363/one-punch-man-Destiny/master/Blood_updates"))();
	end,
	Menu = {
		Information = function(self)
			X.Banner({
				Text = "added: + Blood + Bark and more..."
			})
		end
	}
})

local A = Y.Button({
	Text = "Scripts",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/Minecraft18363/one-punch-man-Destiny/master/scripts"))();
	end,
	Menu = {
		Information = function(self)
			X.Banner({
				Text = "added: + Gui + SS Script + trash"
			})
		end
	}
})

local A = Y.Button({
	Text = "exodus-system",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/Minecraft18363/one-punch-man-Destiny/master/system-exodus.com"))();
	end,
	Menu = {
		Information = function(self)
			X.Banner({
				Text = "Island"
			})
		end
	}
})

local A = C.Button({
	Text = "icecream_Hub (discord)",
	Callback = function()
		setclipboard("")
	end,
	Menu = {
		Information = function(self)
			X.Banner({
				Text = "added: not discord yet"
			})
		end
	}
})

local A = W.Button({
	Text = "icecream_Hub (Key)",
	Callback = function()
		setclipboard("BarKBjyKHoFVRfVoyWCHbfvRGWcVEq")
	end,
	Menu = {
		Information = function(self)
			X.Banner({
				Text = "added: The key is to prevent people from taking the script and leaving. It changes every 2 days!"
			})
		end
	}
})

local A = W.Button({
	Text = "icecream_Hub (games)",
	Callback = function()
		setclipboard("text")
	end,
	Menu = {
		Information = function(self)
			X.Banner({
				Text = "added: + Arsenal +Prison Life +Counter Blox + Whatever Floats Your Boat + Speed Run 4 + Speed Run Simulator + Tower Of Hell + Big Paintball + Unit: Classified + Pistol 1v1 + Nerf FPS + Breaking Point + Flee The Facility + Build A Boat For Treasure + Strucid + Jailbreak + Lumber Tycoon 2 + Vehicle Simulator + Murder Mystery 2 + Critical Strike + The Dropper + Driving Simulator + PolyGuns + Wild West + Beat Up Simulator (Multiplayer) + Shootout + Weaponry"
			})
		end
	}
})
